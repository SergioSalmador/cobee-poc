apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "go-webapp.fullname" . }}-config
  labels:
    {{- include "go-webapp.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
